## Document map/dictionary
If object can have any value as a key, but value is always of the same type, you can document it like this:

```js
/** @type {Object.<string, number[]>}} */
let numbersByGuid;
```
